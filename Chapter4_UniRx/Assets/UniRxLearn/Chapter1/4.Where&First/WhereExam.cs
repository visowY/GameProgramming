using UniRx;
using UnityEngine;

namespace UniRxLearn.Chapter1
{
    public class WhereExam : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }
        //where 在事件的接受者和发布者之间添加一个过滤操作，会过滤掉不满足条件的事件。
        //First 首次事件过滤
    }
}